<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.graduation.doctroidmedical">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".utils.DoctroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".home.ui.activity.ForgetPassword" />
        <activity android:name=".home.ui.activity.MainActivity" />
        <activity android:name=".home.ui.activity.SignUpActivity">

        </activity>
        <activity android:name=".home.ui.activity.SignInActivity" />
        <activity android:name=".on_boarding.ui.OnBoardingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.ui.activity.AppointmentActivity" />
    </application>

</manifest>